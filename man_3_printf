.\" Manpage for _printf.
.TH _printf 3 "17 10 2022" "1.0" "Program Manual for _printf"

.SH NAME
_printf()

The _printf function recreates printf() form stdio.
.SH SYNOPSIS
.B #include "main.h"

.B int _printf(const char \fI*format\fR,
.B ...)

.SH DESCRIPTION
Writes formatted output.

.B Return Value
.RS 3
The function returns the number of characters printed (excluding the null byte), or -1, if error.
.RE

.SH NOTES
The program doesn't account for flags, fields, precision, or length modifiers.

.SH EXAMPLE
.RS 3
To print a sentence with conversion specifiers:

.RE

.RS 8
#include "main.h"

_printf("%s has %d studio albums.", "Kraftwerk", 10);
.RE

.RS 3
Output : "Kraftwerk has 10 studio albums."

.SH EXAMPLE
.R To print the the string \fBHello Patrick!\fR and its length in decimal.

#include ''main.h''

int main(void)

{

	int length;

	_printf(''%s'', ''Hello, Patrick!'')

	_printf(''Hello Patrick! contains %d characters'', length);

	length = _printf(''Hello Patrick!'');

	return (0);

}
.SH SEE ALSO
.R printf(3)

.SH Author 
Samuel Obah                    
